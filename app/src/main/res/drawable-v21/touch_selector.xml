<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!--State when a row is being pressed, but hasnt yet been activated (finger down) -->
    <item android:state_pressed="true">
        <ripple android:color="@color/grey"/>
    </item>

    <!-- When the view is activated. In Single choice mode,it flags the active row of a listview -->
    <item android:state_activated="true"
        android:drawable="@color/sunshine_light_blue" />

    <!-- Default, "just hangin' out" state. -->
    <item android:drawable="@android:color/transparent" />

</selector>